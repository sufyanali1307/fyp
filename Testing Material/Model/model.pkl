from sklearn.linear_model import LinearRegression
import joblib
import numpy as np

model = LinearRegression()
X = np.array([[1],[2],[3]])
y = np.array([2,4,6])
model.fit(X,y)

joblib.dump(model,"model.pkl")